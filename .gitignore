# secrets — never commit
.env

# python
.venv/
__pycache__/
*.pyc

# node
npm-debug.log*

# misc
.DS_Store

# IDE
.kilo

# data — raw parquets (data/) and baked outputs (src/economics/data/) are
# large binaries; regenerate baked data with `make precompute`.
# SAM fixtures live in tests/events/ (tracked).
data/
src/economics/data/
.aws-sam/

# cache
.ruff_cache
.pytest_cache

# cdk
cdk.out/